require 'spec_helper'

describe ResourceNameValidator do
  it 'has a version number' do
    expect(ResourceNameValidator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
